name: Deploy Frontend

on:
  workflow_dispatch: # Ручной запуск
  repository_dispatch: # Событие от tclient репозитория
    types: [deploy-frontend]

jobs:
  deploy-frontend:
    runs-on: ubuntu-latest

    steps:
      - name: Get frontend image info
        id: image-info
        run: |
          # Получаем информацию о новом образе из payload
          FRONTEND_IMAGE="${{ github.event.client_payload.frontend_image }}"
          echo "frontend_image=$FRONTEND_IMAGE" >> $GITHUB_OUTPUT
          echo "🚀 Deploying frontend image: $FRONTEND_IMAGE"

      - name: Deploy to production server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            echo "🚀 Deploying updated frontend..."

            cd /root/tattoo-app

            # Останавливаем frontend
            echo "⏹️ Stopping frontend service..."
            docker-compose -f docker-compose.prod.yml stop frontend

            # Удаляем старый образ
            echo "🗑️ Removing old frontend image..."
            docker rmi mip92/tattoo-client:latest || true

            # Запускаем frontend с новым образом
            echo "🔄 Starting frontend with new image..."
            docker-compose -f docker-compose.prod.yml up -d frontend

            # Ждем инициализации
            echo "⏳ Waiting for frontend to initialize..."
            sleep 20

            # Проверяем статус
            echo "📊 Checking frontend status..."
            docker ps | grep frontend

            echo "✅ Frontend deployment completed!"

      - name: Health check frontend
        run: |
          echo "🔍 Testing frontend health..."
          sleep 10

          # Проверяем main page
          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" http://${{ secrets.SERVER_IP }}/)

          if [ "$RESPONSE" = "200" ]; then
            echo "✅ Frontend is healthy (HTTP $RESPONSE)"
          else
            echo "❌ Frontend health check failed (HTTP $RESPONSE)"
            exit 1
          fi
